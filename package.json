{
  "name": "data-token-holders",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "bun test.ts",
    "upDB": "docker-compose up -d",
    "downDB": "docker-compose stop && docker-compose rm && sudo rm -rf pgdata",
    "start": "docker-compose up -d && sleep 2 && bun index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.9.0",
    "bun": "^1.0.11"
  },
  "dependencies": {
    "ccxt": "^4.2.18",
    "dotenv": "^16.3.1",
    "graphql-request": "^6.1.0",
    "pg": "^8.11.3"
  }
}
